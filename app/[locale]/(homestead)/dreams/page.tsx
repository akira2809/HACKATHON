// ============================================================
// Screen: Dream Tracker
// Route: (homestead)/dreams/page.tsx
// Layout: (homestead)/layout.tsx (TopNav + BottomNav via pathname)
// ============================================================
// Server Component — interactive parts extracted to DreamsContent.tsx

import { DreamsContent } from '../DreamsContent';

export default function DreamsPage() {
  return <DreamsContent />;
}
