// ============================================================
// Screen: Adventures / Quest Board
// Route: (homestead)/adventures/page.tsx
// Layout: (homestead)/layout.tsx (TopNav + BottomNav via pathname)
// ============================================================
// Server Component — interactive parts extracted to AdventuresContent.tsx

import { AdventuresContent } from '../AdventuresContent';

export default function AdventuresPage() {
  return <AdventuresContent />;
}
