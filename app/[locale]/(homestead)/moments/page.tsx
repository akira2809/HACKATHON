// ============================================================
// Screen: Family Moments
// Route: (homestead)/moments/page.tsx
// Layout: (homestead)/layout.tsx (TopNav + BottomNav via pathname)
// ============================================================
// Server Component — interactive parts extracted to MomentsContent.tsx

import { MomentsContent } from '../MomentsContent';

export default function MomentsPage() {
  return <MomentsContent />;
}
